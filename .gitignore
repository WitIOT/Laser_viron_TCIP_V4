# Python
__pycache__/
*.py[cod]
.pytest_cache/

# PyInstaller build output
build/
dist/

# Inno Setup output
installer_output/

# Release zip artifacts
release_assets/

# Runtime state (มี IP/user ของอุปกรณ์ + ข้อมูลรัน ไม่ควรอยู่ใน git)
setting/
logs/

# Old project snapshots (runtime data — keep on disk, out of git)
old/

# Mermaid intermediate files
docs/_mmd/
